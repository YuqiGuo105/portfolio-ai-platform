name: Deploy portfolio-agent-service

on:
  # Auto-deploy when CI passes on main (green-gate)
  workflow_run:
    workflows: ["CI \u2013 Build & Test"]
    branches: [main]
    types: [completed]

  workflow_dispatch:
    inputs:
      image_tag:
        description: "Image tag override (optional). Defaults to sha-<short-sha>."
        required: false
        type: string
  push:
    tags:
      - "agent-v*"

permissions:
  contents: read
  id-token: write

env:
  PROJECT_ID:        ${{ vars.GCP_PROJECT_ID }}
  REGION:            ${{ vars.GCP_REGION }}
  ARTIFACT_REPO:     ${{ vars.ARTIFACT_REPO }}
  IMAGE_NAME:        portfolio-agent-service
  SERVICE_NAME:      portfolio-agent-service
  WIF_PROVIDER:      ${{ vars.WIF_PROVIDER }}
  DEPLOYER_SA_EMAIL: ${{ vars.DEPLOYER_SA_EMAIL }}
  RUNTIME_SA_EMAIL:  ${{ vars.AGENT_RUNTIME_SA_EMAIL }}
  ALLOWED_ORIGINS:   ${{ vars.ALLOWED_ORIGINS }}

jobs:
  deploy:
    # When triggered by workflow_run, only proceed if CI succeeded.
    if: >
      github.event_name != 'workflow_run' ||
      github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven

      - name: Build agent service JAR
        run: |
          mvn -B -DskipTests=false -pl portfolio-agent-service -am package

      - name: Compute image tag
        id: tag
        run: |
          if [ -n "${{ github.event.inputs.image_tag }}" ]; then
            TAG="${{ github.event.inputs.image_tag }}"
          elif [[ "${GITHUB_REF}" == refs/tags/agent-v* ]]; then
            TAG="${GITHUB_REF#refs/tags/}"
          else
            TAG="sha-$(echo "${GITHUB_SHA}" | cut -c1-7)"
          fi
          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"
          echo "Resolved image tag: ${TAG}"

      - name: Authenticate to Google Cloud (Workload Identity Federation)
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ env.WIF_PROVIDER }}
          service_account: ${{ env.DEPLOYER_SA_EMAIL }}

      - name: Set up gcloud
        uses: google-github-actions/setup-gcloud@v2

      - name: Configure Docker for Artifact Registry
        run: gcloud auth configure-docker ${{ env.REGION }}-docker.pkg.dev --quiet

      - name: Build & push image
        run: |
          IMAGE="${REGION}-docker.pkg.dev/${PROJECT_ID}/${ARTIFACT_REPO}/${IMAGE_NAME}:${{ steps.tag.outputs.tag }}"
          docker build \
            -f portfolio-agent-service/Dockerfile \
            -t "${IMAGE}" .
          docker push "${IMAGE}"
          echo "IMAGE=${IMAGE}" >> "$GITHUB_ENV"

      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy "${SERVICE_NAME}" \
            --image="${IMAGE}" \
            --region="${REGION}" \
            --project="${PROJECT_ID}" \
            --service-account="${RUNTIME_SA_EMAIL}" \
            --allow-unauthenticated \
            --port=8090 \
            --min-instances=0 \
            --max-instances=2 \
            --memory=512Mi \
            --cpu=1 \
            --timeout=120 \
            --set-env-vars="^|^SPRING_PROFILES_ACTIVE=prod|ALLOWED_ORIGINS=${ALLOWED_ORIGINS}|MCP_GATEWAY_BASE_URL=${{ vars.MCP_GATEWAY_BASE_URL }}|AGENT_INTENT_PROVIDER=${{ vars.AGENT_INTENT_PROVIDER }}|OPENAI_INTENT_MODEL=${{ vars.OPENAI_INTENT_MODEL }}|OPENAI_INTENT_ESCALATION_MODEL=${{ vars.OPENAI_INTENT_ESCALATION_MODEL }}|GEMINI_INTENT_MODEL=${{ vars.GEMINI_INTENT_MODEL }}|GEMINI_INTENT_ESCALATION_MODEL=${{ vars.GEMINI_INTENT_ESCALATION_MODEL }}" \
            --remove-secrets="AIVEN_PG_PASSWORD" \
            --update-secrets="OPENAI_API_KEY=OPENAI_API_KEY:latest,GEMINI_API_KEY=GEMINI_API_KEY:latest,MCP_GATEWAY_INTERNAL_TOKEN=MCP_GATEWAY_INTERNAL_TOKEN:latest,SPRING_DATASOURCE_URL=SPRING_DATASOURCE_URL:latest,SPRING_DATASOURCE_USERNAME=SPRING_DATASOURCE_USERNAME:latest,SPRING_DATASOURCE_PASSWORD=SPRING_DATASOURCE_PASSWORD:latest,SUPABASE_JWT_SECRET=SUPABASE_JWT_SECRET:latest,REDIS_URL=REDIS_URL:latest,OPENSEARCH_PASSWORD=OPENSEARCH_PASSWORD:latest"

      - name: Smoke test (wait for ready revision)
        run: |
          for i in $(seq 1 30); do
            LATEST_READY=$(gcloud run services describe "${SERVICE_NAME}" \
              --region="${REGION}" --project="${PROJECT_ID}" \
              --format='value(status.latestReadyRevisionName)')
            LATEST_CREATED=$(gcloud run services describe "${SERVICE_NAME}" \
              --region="${REGION}" --project="${PROJECT_ID}" \
              --format='value(status.latestCreatedRevisionName)')
            echo "ready=${LATEST_READY} created=${LATEST_CREATED}"
            if [ -n "${LATEST_READY}" ] && [ "${LATEST_READY}" = "${LATEST_CREATED}" ]; then
              echo "Revision ${LATEST_READY} is ready."
              exit 0
            fi
            sleep 5
          done
          echo "Latest revision never became ready in time."
          exit 1
